# Display
# Driver ST7789 dipakai dari https://github.com/pkkirilov/ST7789 (bukan
# paket pimoroni "st7789" di PyPI) — cocok untuk modul 7-pin tanpa CS pin
# yang dipasang di robot ini. Tidak ada rilis PyPI resmi, jadi divendor
# manual ke venv oleh setup.sh (lihat fungsi install_st7789_driver).
Adafruit-GPIO>=1.0.3
spidev>=3.6
Pillow>=10.0.0

# GPIO
RPi.GPIO>=0.7.1
gpiozero>=2.0

# Audio
pygame>=2.5.0
pyaudio>=0.2.13

# Vision (Phase 2)
# Belum ada wheel prebuilt di piwheels untuk Python 3.13/aarch64, jadi pip
# akan compile dari source (~1GB+ ruang sementara) — berat & rawan bikin
# "No space left on device" di Pi Zero 2W. Tidak ada modul Phase 1 yang
# import cv2, jadi aman ditunda. Pas mulai Phase 2 (mata/), install lewat
# apt dulu: `sudo apt install python3-opencv` (prebuilt, jauh lebih ringan)
# baru uncomment baris ini kalau versi apt-nya kurang baru buat kebutuhanmu.
# opencv-python-headless>=4.8.0 # uncomment Phase 2
# face_recognition>=1.3.0 # uncomment Phase 2
# picamera2>=0.3.0 # uncomment Phase 2

# AI (Phase 3)
# openai>=1.0.0 # uncomment Phase 3

# Utils
numpy>=1.24.0
dataclasses-json>=0.6.0
